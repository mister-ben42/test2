
{% extends "MDQGeneBundle:Gene:layoutHighSc.html.twig" %}



{% block liste_scores %}

		
		 {% for scuser in scusers %}

			
			{% set number=loop.index+(pagi.page-1)*pagi.nbParPage %}
		<div class="ligne_HSc_Med">
		<span class="num_nom" >{{number}}- 
		<a href="{{ path('mdquser_profileU', {'id':scuser.id}) }}" {%if scuser.id==id_connect %}style="color:rgb(128,255,255);"{%elseif scuser.id==id_search %} style="color:rgb(255,168,168);" {%endif%}> {{scuser.username}} </a>
		</span>
		<span class="score">
			{% if scuser.ar1+scuser.ar2+scuser.ar3+scuser.ar4+scuser.ar5==0 %}
				Meilleur classement : {{scuser.highClassDayAr}}ème
			{% else %}
				<span class="bloc_med">
					{% if scuser.ar1>0 %}
					<img src={{ asset('bundles/UserBundle/MedOr0.png') }} alt="Med">x{{scuser.ar1}}
					{% endif %}
				</span>
				<span class="bloc_med">
					{% if scuser.ar2>0 %}
					<img src={{ asset('bundles/UserBundle/MedAg0.png') }} alt="Med">x{{scuser.ar2}}
					{% endif %}
				</span>
				<span class="bloc_med">
					{% if scuser.ar3>0 %}
					<img src={{ asset('bundles/UserBundle/MedBz0.png') }} alt="Med">x{{scuser.ar3}}
					{% endif %}
				</span>
				<span class="bloc_med">
					{% if scuser.ar4>0 %}
					<img src={{ asset('bundles/UserBundle/MedCh0.png') }} alt="Med">x{{scuser.ar4}}
					{% endif %}
				</span>
				<span class="bloc_med">
					{% if scuser.ar5>0 %}
					<img src={{ asset('bundles/UserBundle/MedCt0.png') }} alt="Med">x{{scuser.ar5}}
					{% endif %}
				</span>
			{% endif %}
		</span></div>
		 {% endfor %}

{% endblock %}
